---
# handlers file for db

- name: db restart
  ansible.builtin.service:
    name: mariadb
    state: restarted

- name: db reload
  ansible.builtin.service:
    name: mariadb
    state: reloaded

- name: daemon reload
  ansible.builtin.systemd_service:
    daemon_reload: treu
    